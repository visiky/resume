name: github pages

on:
  push:
    branches:
      - master # Set a branch name to trigger deployment

jobs:
  deploy:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 12

      - run: yarn install
      - run: npm run build
<<<<<<< Updated upstream
      - run: |
          git config --local user.name visiky
          git config --local user.email 736929286@qq.com
          npm run deploy
=======

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          
>>>>>>> Stashed changes
